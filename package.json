{
  "name": "twinmind-live",
  "version": "0.1.0",
  "private": true,
  "description": "Session-based live mic capture, chunked Groq Whisper transcription, GPT-OSS suggestions and chat.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:api dev:web",
    "dev:api": "npm --workspace @twinmind/api run dev",
    "dev:web": "npm --workspace @twinmind/web run dev",
    "build": "npm --workspace @twinmind/shared run build && npm --workspace @twinmind/api run build && npm --workspace @twinmind/web run build",
    "start:api": "npm --workspace @twinmind/api run start",
    "start:web": "npm --workspace @twinmind/web run start",
    "typecheck": "npm --workspaces --if-present run typecheck"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
